- Is the term "intermediate polynomials" in use in the code? Are they _only_ used for lookups and permutations? Same for "FRI polynomials" 

- For each of the polynomial types, provide references 

- What is LinearHash?

- Why 63 bits per field element?

- Why do we have (alpha, gamma, ...) in stage too.. is it actually more  challanges than those two?

- What is the actual vvalue of J in terms of the number of air constraints? Does it depend on which chip or recursion step we're in?

- What does "for each polynomial p and offset o" mean, need to be more specific what we're iterating over. Also the {e_{p,o}} mean?

- What are the offsets? more detail needed.

- 
