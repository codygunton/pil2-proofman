pil2-proofman is a library SNARK library used by the zisk zkvm.

To simplify your focus, you make use of the scripts setup.sh, build.sh and run-proofman.sh to run a set if simple tests. We will eventually add a run.sh script that will run our main dev iteration step. There is also a clean.sh script that will likely not be necessary.
