[package]
name = "pinning"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
sha2 = "0.10"
hex = "0.4"
serde_json = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
# No additional dev dependencies needed - tests use std::process
